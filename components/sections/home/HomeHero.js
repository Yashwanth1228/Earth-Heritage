import HeroSection from '@/components/sections/landing/HeroSection';

/**
 * HomeHero — Re-exports the signature landing page HeroSection
 * ensuring complete visual and functional parity for the Earth Heritage Home page.
 */
export default function HomeHero() {
  return <HeroSection />;
}
